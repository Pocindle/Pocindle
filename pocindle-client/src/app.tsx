import React from 'react';
import AppRoute from './routes/appRoute';

const App: React.FC = () => {
  return (
    <React.Fragment>
      <AppRoute />
    </React.Fragment>
  );
};

export default App;
